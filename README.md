# Purity Dash 🚀
A modern, responsive Admin Dashboard built with **React**, **Tailwind CSS**, and **Lucide Icons**.

## ✨ Features
- **Full CRUD Operations**: Add and delete authors directly from the table.
- **Real-time Search**: Instant filtering of data through the dynamic search bar.
- **Data Persistence**: Uses `localStorage` to keep your data saved even after page refresh.
- **Responsive Design**: Clean UI that works on different screen sizes.
- **Dynamic Stats**: Real-time counter for the total number of authors.

## 🛠️ Tech Stack
- React (Hooks, UseEffect, UseState)
- Tailwind CSS (Styling)
- Lucide React (Icons)
- JSONPlaceholder (Initial Data API)

## 🚀 How to Run
1. Clone the repo: `git clone [YOUR_REPO_URL]`
2. Install dependencies: `npm install`
3. Start the project: `npm run dev`
