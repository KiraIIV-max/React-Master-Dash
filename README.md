# Purity Dash 🚀
A modern, responsive Admin Dashboard built with **React**, **Tailwind CSS**, and **Lucide Icons**.

[🔗 Live Demo](https://react-master-dash.vercel.app)

## ✨ Features
- **Full CRUD Operations**: Add and delete authors directly from the table with instant state updates.
- **Real-time Search**: Instant filtering of data through the dynamic search bar using React state.
- **Data Persistence**: Uses `localStorage` to keep your data saved even after page refresh (Safety-checked JSON parsing).
- **Responsive Design**: Mobile-first UI with a toggleable sidebar and horizontal scrollable tables for small screens.
- **Dynamic Stats**: Real-time counters that automatically reflect the current number of filtered authors.

## 🛠️ Tech Stack
- **Framework**: React (Functional Components & Hooks: `useState`, `useEffect`)
- **Styling**: Tailwind CSS (Utility-first CSS, Responsive Design)
- **Icons**: Lucide React
- **API**: JSONPlaceholder (Initial Data Fetching)
- **Deployment**: Vercel

## 🚀 How to Run
1. Clone the repo: `git clone [YOUR_REPO_URL]`
2. Install dependencies: `npm install`
3. Start the project: `npm run dev`
